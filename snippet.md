在 snippet 的 SQL 內容中直接使用  ${名稱}  語法即可

DBX 使用 CodeMirror 的  snippetCompletion  引擎（見  QueryEditor.vue  第 3079 行），完全支援標準 tab-stop 佔位符語法。

你的範例

在 設定 → 程式碼片段 → 新增片段 中填入：

┌──────────┬─────────────────────────────────────────────────┐
│ 欄位     │ 值                                              │
├──────────┼─────────────────────────────────────────────────┤
│ 顯示名   │ select with condition                           │
├──────────┼─────────────────────────────────────────────────┤
│ 觸發鍵   │ swc                                             │
├──────────┼─────────────────────────────────────────────────┤
│ SQL 內容 │ SELECT a, b, c FROM table1 WHERE a = '${value}' │
└──────────┴─────────────────────────────────────────────────┘

使用時輸入  swc  → 選取片段 → SQL 展開後，游標會停在  ${value}  位置，直接輸入要查的值，按 Tab 結束。

多個佔位符 + Tab 順序

用  ${編號:預設值}  控制 Tab 跳躍順序：

SELECT ${1:a}, ${2:b}, ${3:c}
FROM ${4:table1}
WHERE ${1:a} = '${5:xxx}'

Tab 順序： 1  →  2  →  3  →  4  →  5  → 結束

 ${1:a}  出現兩次代表連動（mirror）：在 SELECT 的  a  改了，WHERE 的  a  也跟著變。

實用範例

範例 1：帶條件的萬用查詢

觸發鍵：sfw
SQL 內容：
SELECT *
FROM ${table}
WHERE ${column} = '${value}'
LIMIT 100;

範例 2：INSERT 模板

觸發鍵：insv
SQL 內容：
INSERT INTO ${table} (${columns})
VALUES (${values});

範例 3：帶日期範圍的查詢

觸發鍵：sdr
SQL 內容：
SELECT ${1:id}, ${2:created_at}, ${3:status}
FROM ${4:orders}
WHERE ${2:created_at} BETWEEN '${5:2024-01-01}' AND '${6:2024-12-31}'

語法對照表

┌─────────────────┬─────────────────────────────────────────────────┐
│ 語法            │ 效果                                            │
├─────────────────┼─────────────────────────────────────────────────┤
│ ${name}         │ 佔位符，Tab 跳到這裡讓你輸入                    │
├─────────────────┼─────────────────────────────────────────────────┤
│ ${1:default}    │ 帶編號和預設值的佔位符，按編號順序 Tab          │
├─────────────────┼─────────────────────────────────────────────────┤
│ ${1:x} 重複出現 │ 連動佔位符，改一處全部同步更新                  │
├─────────────────┼─────────────────────────────────────────────────┤
│ $0              │ 最終游標停留位置（所有 Tab 結束後游標落在這裡） │
└─────────────────┴─────────────────────────────────────────────────┘

注意事項

• 自訂片段（非  builtin-  開頭）的  ${...}  會原樣保留，不會被轉換或移除（見  sqlCompletion.ts  第 3234 行： if (!shouldFormatBuiltinSnippet(snippet)) return body; ）
•  $  符號如果需要在 SQL 中作為一般文字（如 PostgreSQL dollar-quoted string  $$ ），可能會被 CodeMirror 誤判為佔位符。遇到這種情況，用  \${}  跳脫或避免在 snippet 中使用  $  字面量